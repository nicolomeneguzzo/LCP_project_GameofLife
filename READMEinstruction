#READMEinstruction

Work instruction for coding flow:
Un po di comandi e istruzioni comode che mi dimentico sempre.

set up:

`git switch develop`
`git branch --set-upstream-to=origin/develop develop`

git routine:

aggiornare branch locale
`git switch develop
`git pull`

crea un branch per lavorare, (crei un branch ramificato da develop con tutti i file di develop)
`git switch -c nome.branch`
|
|
|
finito di lavorare
per apportare le modifiche in develop:
`git switch develop`
`git merge nome.branch`

controlla che il branch develop si sia aggiornato, quando hai finito puoi cancellare il branch con 
`git branch -D nome.branch`

quindi esegui la santa trinità:
`git add`
`git commit -m 'frase'`
`git push`
